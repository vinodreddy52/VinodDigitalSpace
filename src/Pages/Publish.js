import React from "react";
import { Container, Typography } from "@mui/material";

const Publish = () => (
  <Container>
    <Typography variant="h4">Published Works</Typography>
  </Container>
);

export default Publish;
